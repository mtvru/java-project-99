spring:
  datasource:
    url: jdbc:h2:file:./data/devdb;AUTO_SERVER=TRUE
    username: user
    password: 123

  h2:
    console:
      enabled: true
      path: /h2-console

  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
    show-sql: true
    generate-ddl: true
    properties:
      hibernate:
        format_sql: true
